---
# Bootstrap a machine using the common role
- name: Configure the SSH users, sudo and other common things
  hosts: all
  become: false
  remote_user: root
  tags:
    - common
  roles:
    - { role: common,
        users: "{{ nlnog_users }}",
        user_groups: "{{ nlnog_groups }}" }

